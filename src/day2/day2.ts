
type Rock = 'A'  | 'X';
type Paper = 'B' | 'Y';
type Scissors = 'C' | 'Z';

type Hand = Rock|Paper|Scissors;

const matchPoints:Record<string, number> = {
    'win': 6,
    'loss': 0,
    'draw': 3
}

const handPoints:Record<Hand, number> = {
    'A'| 'X'): 
}
export function calculateWinner(a:Hand,b:Hand):string {
    
 }

 export function 
